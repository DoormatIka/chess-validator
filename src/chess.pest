game = { "position"? ~ "fen" ~ fen ~ ("moves" ~ moves)? }
fen = { (!("moves" | "fen") ~ ASCII)+ }
moves = { (!"moves" ~ move_part)+ }
move_part = @{ (('a'..'f') ~ ('1'..'8')){2} ~ ("p" | "P" | "n" | "N" | "b" | "B" | "r" | "R" | "q" | "Q" | "k" | "K")? }
WHITESPACE = _{" "}
